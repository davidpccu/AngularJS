import { ShowitDirective } from './showit.directive';

describe('ShowitDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowitDirective();
    expect(directive).toBeTruthy();
  });
});
